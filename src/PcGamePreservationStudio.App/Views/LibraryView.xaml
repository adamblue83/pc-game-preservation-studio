<UserControl x:Class="PcGamePreservationStudio.App.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Library" Style="{StaticResource HeadingTextStyle}" />

        <DockPanel Grid.Row="1" Margin="0,0,0,16" LastChildFill="True">
            <Button DockPanel.Dock="Right" Content="Add GOG Offline Installer…" Margin="8,0,0,0" Command="{Binding AddGogOfflineInstallerCommand}" />
            <Button DockPanel.Dock="Right" Content="Add Local Folder…" Margin="8,0,0,0" Command="{Binding AddLocalFolderCommand}" />
            <Button DockPanel.Dock="Right" Content="Refresh" Margin="8,0,0,0" Command="{Binding RefreshCommand}" />
            <ComboBox DockPanel.Dock="Right" Width="150" Margin="8,0,0,0"
                      ItemsSource="{Binding PlatformFilterOptions}" SelectedItem="{Binding SelectedPlatformFilter}"
                      DisplayMemberPath="Label" />
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center">
                <TextBox.Style>
                    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </DockPanel>

        <Grid Grid.Row="2">
            <TextBlock Text="Detecting games…" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                       Style="{StaticResource SecondaryTextStyle}" HorizontalAlignment="Left" VerticalAlignment="Top" />

            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                       TextWrapping="Wrap" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,12" />

            <TextBlock Text="No games detected yet. Try Refresh, or Add Local Folder to archive a game manually."
                       TextWrapping="Wrap"
                       HorizontalAlignment="Left" VerticalAlignment="Top">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource SecondaryTextStyle}">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsLoading}" Value="False" />
                                    <Condition Binding="{Binding AllGames.Count}" Value="0" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,32,0,0">
                <ItemsControl ItemsSource="{Binding GamesView}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.SelectGameCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"
                                    Margin="0,0,12,12" Padding="0" HorizontalContentAlignment="Stretch">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                        <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
                                    </Style>
                                </Button.Style>
                                <Grid Width="200">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="110" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="0" Background="{StaticResource SurfaceAltBrush}" CornerRadius="4,4,0,0">
                                        <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   TextAlignment="Center" TextWrapping="Wrap" Margin="8" MaxHeight="94" TextTrimming="CharacterEllipsis" />
                                    </Border>
                                    <StackPanel Grid.Row="1" Margin="10,8">
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="3" Padding="6,1" Margin="0,0,6,0">
                                                <TextBlock Text="{Binding PlatformLabel}" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" />
                                            </Border>
                                            <TextBlock Text="{Binding SizeLabel}" Style="{StaticResource SecondaryTextStyle}" />
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
