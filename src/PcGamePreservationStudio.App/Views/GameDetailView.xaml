<UserControl x:Class="PcGamePreservationStudio.App.Views.GameDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <TextBlock Text="Loading…" Style="{StaticResource SecondaryTextStyle}"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                   HorizontalAlignment="Left" VerticalAlignment="Top" />

        <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                   HorizontalAlignment="Left" VerticalAlignment="Top" />

        <StackPanel Visibility="{Binding Detail, Converter={StaticResource NullToVisibilityConverter}}"
                    MaxWidth="640" HorizontalAlignment="Left">
            <TextBlock Text="{Binding Detail.Entry.Title}" Style="{StaticResource HeadingTextStyle}" TextWrapping="Wrap" />

            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Platform" Style="{StaticResource SecondaryTextStyle}" Margin="0,0,0,10" />
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Detail.Entry.Platform, Converter={StaticResource GamePlatformToLabelConverter}}" Margin="0,0,0,10" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="App ID" Style="{StaticResource SecondaryTextStyle}" Margin="0,0,0,10" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Detail.Entry.PlatformAppId}" Margin="0,0,0,10" TextTrimming="CharacterEllipsis" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Install folder" Style="{StaticResource SecondaryTextStyle}" Margin="0,0,0,10" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Detail.Entry.InstallPath}" Margin="0,0,0,10" TextWrapping="Wrap" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Manifest" Style="{StaticResource SecondaryTextStyle}" Margin="0,0,0,10" />
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Detail.Entry.ManifestPath}" Margin="0,0,0,10" TextWrapping="Wrap" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Installed size" Style="{StaticResource SecondaryTextStyle}" Margin="0,0,0,10" />
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Detail.Entry.SizeOnDiskBytes, Converter={StaticResource BytesToFriendlySizeConverter}}" Margin="0,0,0,10" />

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Build ID" Style="{StaticResource SecondaryTextStyle}" />
                    <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding Detail.BuildId}" />
                </Grid>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Preservation analysis" FontWeight="SemiBold" Margin="0,0,0,6" />

                    <TextBlock Text="Analyzing…" Style="{StaticResource SecondaryTextStyle}"
                               Visibility="{Binding IsAnalyzing, Converter={StaticResource BoolToVisibilityConverter}}" />

                    <StackPanel Visibility="{Binding DrmAnalysis, Converter={StaticResource NullToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{Binding DrmAnalysis.Label, Converter={StaticResource DrmAnalysisLabelToTextConverter}}" FontWeight="SemiBold" />
                            <TextBlock Text=" · " Style="{StaticResource SecondaryTextStyle}" />
                            <TextBlock Text="{Binding DrmAnalysis.Confidence, Converter={StaticResource DrmAnalysisConfidenceToTextConverter}}" Style="{StaticResource SecondaryTextStyle}" />
                        </StackPanel>

                        <ItemsControl ItemsSource="{Binding DrmAnalysis.Findings}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,8">
                                        <TextBlock Text="{Binding Description}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding Explanation}" Style="{StaticResource SecondaryTextStyle}" TextWrapping="Wrap" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="This is evidence, not a guarantee — always test an archived copy offline before relying on it."
                                   Style="{StaticResource SecondaryTextStyle}" FontStyle="Italic" TextWrapping="Wrap" Margin="0,4,0,0" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Button Content="Archive…" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Left" Command="{Binding ArchiveCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
