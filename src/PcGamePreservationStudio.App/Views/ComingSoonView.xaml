<UserControl x:Class="PcGamePreservationStudio.App.Views.ComingSoonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <StackPanel MaxWidth="520" HorizontalAlignment="Left" VerticalAlignment="Center">
        <Border Style="{StaticResource CardBorderStyle}">
            <StackPanel>
                <TextBlock Text="{Binding FeatureName}" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,4" />
                <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="3" Padding="8,2" HorizontalAlignment="Left" Margin="0,0,0,12">
                    <TextBlock Text="Coming Soon" FontSize="11" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold" />
                </Border>
                <TextBlock Text="{Binding Explanation}" TextWrapping="Wrap" Style="{StaticResource SecondaryTextStyle}" />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
