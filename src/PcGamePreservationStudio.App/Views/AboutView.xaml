<UserControl x:Class="PcGamePreservationStudio.App.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <StackPanel MaxWidth="640" HorizontalAlignment="Left">
        <Image Source="/Assets/AppLogo.png" Width="96" Height="96" HorizontalAlignment="Left" Margin="0,0,0,16" />
        <TextBlock Text="{Binding ApplicationName}" Style="{StaticResource HeadingTextStyle}" />
        <TextBlock Style="{StaticResource SecondaryTextStyle}" Margin="0,0,0,16">
            <Run Text="Version " /><Run Text="{Binding Version, Mode=OneWay}" />
        </TextBlock>

        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
            <TextBlock Text="{Binding LegalNotice}" TextWrapping="Wrap" />
        </Border>

        <TextBlock Text="{Binding License}" Style="{StaticResource SecondaryTextStyle}" />
    </StackPanel>
</UserControl>
